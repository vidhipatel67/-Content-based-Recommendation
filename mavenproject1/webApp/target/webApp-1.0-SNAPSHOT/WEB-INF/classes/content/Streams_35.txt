<p><b>To do:</b><br> Illustrate how the <code>print()</code> method works, emphasize that this method does not break up lines, but that this can be accomplished with the <code>\n</code> escape sequence. Talk about how it works with different data types, and then introduce the <code>println()</code> method as a convenience method that automatically adds the <code>\n</code> character.</p>